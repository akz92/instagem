$('#photos-wrapper').append("<%= j render 'photos', photos: @recent_media[:photos] %>");

$('#photos-pagination-wrapper').html("<%= j render 'photos_next_page', next_max_id: @recent_media[:next_media_id] %>");
